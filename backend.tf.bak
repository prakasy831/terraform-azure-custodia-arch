terraform {
  backend "azurerm" {
    resource_group_name  = "Test-Custodia-Tfm"
    storage_account_name = "tfstatecustodia831"  # ← update here
    container_name       = "tfstate"
    key                  = "terraform.tfstate"
  }
}

